Create one set of video components from these selected records.

Content requirements:
{{ content_instruction }}

Scene:
{{ scene }}

Trigger event:
{{ trigger_event }}

Psychological background:
{{ psychological_background }}

True state for reasoning only:
{{ true_emotion }}

Apparent state for reasoning only:
{{ apparent_emotion }}

Content scene supplement:
{{ content_scene_supplement }}

Style instruction:
{{ style_instruction }}

Preset scene supplement:
{{ preset_scene_supplement }}

Video background:
Scene: {{ background.scene }}
Ambient audio: {{ background.ambient_audio }}
Relationship: {{ background.relationship }}
Lighting: {{ background.lighting }}
Framing: {{ background.framing_supplement }}

Subject:
Age: {{ age }}
Gender: {{ gender }}
Population: {{ ethnicity }}

Positive examples for component writing only:
{% for example in positive_examples %}{{ loop.index }}. {{ example }}
{% else %}None
{% endfor %}

Examples and selected records are reference material only. Rewrite any past tense or certainty wording and never copy text that conflicts with the system rules.

Negative examples for component writing only:
{% for example in negative_examples %}{{ loop.index }}. {{ example }}
{% else %}None
{% endfor %}

{{ policy.output_rule }}

Write the reviewer description first in your reasoning, then derive spokenText, visualBehavior, vocalDelivery and setting from the same event. Keep the selected background, framing, lighting and style recognizable. The application inserts the subject demographics, exact spokenText, fixed close-up camera rule and fixed bright-lighting rule without repairing your fields. Perform the required validation internally and return only the final JSON.
