Target:
{{ target_source }}

User requirement:
{{ user_requirement }}

Current visible form:
{{ current_form_json }}

The complete missing field list that must be returned unchanged:
{{ required_missing_json }}

Available catalog records:
{{ catalog_json }}

Recent readable failure reasons:
{{ failure_reasons_json }}

Return one strict suggestion. Existing records must use their exact id, revision and label. New records must remain Draft.
